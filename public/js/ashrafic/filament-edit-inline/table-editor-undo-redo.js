// Undo/redo functionality integrated into Alpine.store('tableEditor')
// The store handles undo/redo directly via pushUndo(), undo(), redo()
// store.undoStack and store.redoStack are initialized in table-editor.js
